# Review: {{feature_slug}}

> Generated by `/review:full` (Shot 2). Runs in a **fresh session** — never the same session as `/impl:full`.
> Score: 4 dimensions × 25 pts = 100 pts total.
> Dimension 4 differs by project: **BE = Security** · **FE = A11y + Perf**

---

## Verdict

| Threshold | Meaning | Next step |
|-----------|---------|-----------|
| ≥ 90 | **APPROVE** | Proceed to `/docs:full` |
| 75–89 | **MINOR** | Fix listed issues; re-run review; may proceed after fixes |
| 60–74 | **REWORK** | Return to `/impl:full` |
| < 60 or P0 fail | **BLOCK** | Significant rework; human must review |

**Total score: {{N}} / 100**
**Verdict: APPROVE / MINOR / REWORK / BLOCK**

---

## P0 Trait Gates

> A P0 gate failure overrides the numeric verdict → **BLOCK** regardless of score.

| Trait | Gate | Result |
|-------|------|--------|
| `ACCESS_CONTROLLED` | Every NestJS endpoint has `@UseGuards(JwtAuthGuard)` + `@Roles(...)` / every FE route is OIDC-protected | ✓ PASS / ✗ FAIL |
| `WORKFLOW_DRIVEN` | All state transitions tested in TC.md and covered by tests | ✓ PASS / ✗ FAIL |

**P0 gate result:** ALL PASS / FAIL ({{which gate, which file:line}})

---

## Dimension 1 — Fidelity (25 pts)

> Each SC tag verified in the implementation. Score = 25 × (tags verified / total tags).

### SC Tag Coverage Table

| Tag | AC Text | Verified | Notes |
|-----|---------|----------|-------|
| SC-01 | {{AC text}} | ✓ / ✗ | {{notes or file:line}} |
| SC-02 | {{AC text}} | ✓ / ✗ | |

**Fidelity score: {{N}} / 25**

### Fidelity Findings
- [ ] SC-01: {{verified / not found / partially met — file:line}}
- [ ] SC-02: {{...}}

---

## Dimension 2 — Type Health (25 pts)

> TypeScript strict compile · no untyped `any` · DTO / Zod schema alignment.

**Type-check:** `npx tsc --noEmit` — PASS / FAIL
**Untyped `any` count:** {{N}} (0 = full marks; -3 pts each without justifying comment)

### BE: DTO alignment
- [ ] All DTOs use class-validator decorators
- [ ] DTOs match §4 API Contract request/response shapes
- [ ] No raw `any` in controller or service

### FE: Zod schema alignment
- [ ] Zod schemas in `schemas/` match §5 Component Tree prop types
- [ ] TanStack Query return types are typed (no inferred `any`)
- [ ] `z.infer<>` used as component prop type (not a manual interface duplicate)

**Type Health score: {{N}} / 25**

### Type Health Findings
- {{file:line — description — severity}}

---

## Dimension 3 — Coverage (25 pts)

> Test coverage ≥ 85% on changed files · TC.md verdicts filled.

**BE coverage:** `npx jest --coverage` → {{N}}% on changed files (threshold: 85%)
**FE coverage:** `npx vitest run --coverage` → {{N}}% on changed files (threshold: 85%)

### TC.md Verdict Summary

| Tag | Happy | Edge | Error | All Pass |
|-----|-------|------|-------|----------|
| SC-01 | ✓/✗ | ✓/✗ | ✓/✗ | ✓/✗ |
| SC-02 | ✓/✗ | ✓/✗ | ✓/✗ | ✓/✗ |

**Missing test coverage:** {{list files under threshold, or "None"}}

**Coverage score: {{N}} / 25**

### Coverage Findings
- {{file:line — missing test scenario}}

---

## Dimension 4 — Security (BE) / A11y + Perf (FE) (25 pts)

### BE: Security

- [ ] OWASP Top 10 checked (injection, broken auth, sensitive data exposure, XSS, IDOR)
- [ ] Every endpoint has `@UseGuards(JwtAuthGuard)` (unless explicitly public)
- [ ] All DTOs have class-validator on every user-supplied field
- [ ] Prisma queries use parameterized inputs (no string interpolation in `$queryRaw`)
- [ ] No secrets, tokens, or PII in nestjs-pino log output
- [ ] Error responses use `{ success: false, message }` — no stack traces in production

### FE: A11y + Perf

- [ ] WCAG 2.1 AA: every interactive element keyboard-accessible
- [ ] `<button>` (not `<div onClick>`) for clickable elements
- [ ] All images have `alt` text; icons have `aria-label`
- [ ] No unnecessary re-renders (verify with React DevTools profiler notes)
- [ ] TanStack Query `staleTime` set appropriately (not default 0 on high-traffic queries)
- [ ] All user-visible strings use i18next keys (no hardcoded English in JSX)
- [ ] Sentry error boundary wraps new route/page

**Dimension 4 score: {{N}} / 25**

### Dimension 4 Findings
- **Critical** `{{file:line}}` — {{description}} — {{fix}}
- **Important** `{{file:line}}` — {{description}} — {{fix}}
- **Suggestion** `{{file:line}}` — {{description}}

---

## Score Summary

| Dimension | Score | Max |
|-----------|-------|-----|
| Fidelity | {{N}} | 25 |
| Type Health | {{N}} | 25 |
| Coverage | {{N}} | 25 |
| Security / A11y+Perf | {{N}} | 25 |
| **Total** | **{{N}}** | **100** |

---

## Rework Instructions

> Fill only when verdict is REWORK or BLOCK.

**Return to `/impl:full` and fix:**

1. {{Specific fix — file:line — SC tag affected}}
2. {{Specific fix}}

**Do not proceed to `/docs:full` until this review reaches ≥ 90.**
